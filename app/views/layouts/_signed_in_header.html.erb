<% username = current_user.first_name + " " + current_user.last_name %>

<%= link_to( image_tag("estia_logo_color.png", :class => 'header_logo'), current_user) %>

<div class="nav pull-right">

	<%= link_to "Browse", listings_path, :class=>'btn btn-success', style: "font-weight: bold;" %>
	
	<%= link_to username, current_user, :class=>'btn nav-button' %>

	<a href="#" class="btn btn-inverse dropdown " style="font-weight: bold;" data-toggle="dropdown">
		Account <b class="caret"></b>
	</a>
	
	<ul class="dropdown-menu">
		<li><%= link_to "Settings", edit_user_path(current_user) %></li>
		<li class="divider"></li>
		<li><%= link_to "Log Out", logout_path%></li>
	</ul>

</div>